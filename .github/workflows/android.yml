name: Android Build

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    # هذا هو الكشاف 🔦
    # سيطبع لنا كل الملفات وأماكنها الحقيقية
    - name: Detective Mode (List Files)
      run: |
        echo "=== Root Directory Content ==="
        ls -la
        echo "=== App Directory Content ==="
        ls -R

    - name: Build with Gradle
      # عدنا للأمر البسيط مؤقتاً لنرى هل سيعمل بعد اكتشاف الملفات
      run: gradle :app:assembleDebug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: NeuroLog-App
        path: app/build/outputs/apk/debug/app-debug.apk
